# =============================================================================
# Tweede Kamer Pipeline — Smart Data .gitignore
# =============================================================================
# RULE: Code & config are tracked. Data is NEVER tracked.
# Everyone reproduces identical data by running: make data (or python pipeline.py)
# =============================================================================

# ---------------------------------------------------------------------------
# DATA — the big one. Everything under data/ is generated by the pipeline.
# We keep the directory structure via .gitkeep but ignore all actual data.
# ---------------------------------------------------------------------------
# Use glob (*) not directory (/) so the .gitkeep negations below still work.
data/raw/*
data/processed/*

# Catch-all: if anyone puts data files anywhere else in the repo, ignore them.
# Common data formats from this pipeline and downstream analysis.
*.parquet
*.csv
!requirements*.csv
*.json
!package.json
!package-lock.json
*.jsonl
*.ndjson
*.feather
*.arrow
*.h5
*.hdf5
*.pkl
*.pickle
*.sqlite
*.sqlite3
*.db
*.duckdb

# Large data dumps / archives
*.tar.gz
*.tar.bz2
*.tar.xz
*.zip
*.7z
*.gz
!*.tar.gz.dvc

# Excel / spreadsheet (often used as intermediate data)
*.xlsx
*.xls
*.ods

# ---------------------------------------------------------------------------
# PYTHON
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.eggs/
*.so

# Virtual environments (everyone creates their own)
venv/
.venv/
env/
.env/

# ---------------------------------------------------------------------------
# JUPYTER / NOTEBOOKS
# ---------------------------------------------------------------------------
# Ignore notebook checkpoints but track the notebooks themselves
.ipynb_checkpoints/
# Uncomment the next line if you want to ignore notebook outputs too:
# *.ipynb

# ---------------------------------------------------------------------------
# IDE & EDITORS
# ---------------------------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~
.project
.classpath
.settings/

# ---------------------------------------------------------------------------
# OS JUNK
# ---------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
._*

# ---------------------------------------------------------------------------
# LOGS & TEMP
# ---------------------------------------------------------------------------
*.log
logs/
tmp/
temp/
.cache/

# ---------------------------------------------------------------------------
# SECRETS — never commit credentials
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
secrets.yaml
credentials.json

# ---------------------------------------------------------------------------
# EXCEPTIONS — files we DO want to track despite matching patterns above
# ---------------------------------------------------------------------------
# Config file (YAML, not data)
!config.yaml
!config.*.yaml

# Keep directory structure alive
!data/raw/.gitkeep
!data/processed/.gitkeep

# Generated dashboard image
dashboard.png

# Trained models (large binary weights, re-trainable)
models/
*.pt
*.bin
*.safetensors
*.onnx

# Outputs — keep reports, ignore large intermediates
outputs/*.pkl

# Verslag transcript XMLs (large, re-downloadable)
data/texts/*
!data/texts/.gitkeep

# Summary is small & useful for quick reference, but regenerated — your call.
# Uncomment to track it:
# !data/processed/_summary.csv
